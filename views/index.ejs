<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../css/index.css">
    <title>Mongodb6</title>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-4 p-5 bg-secondary">
                <form action="/savedata" method="post" enctype="multipart/form-data">

                    <input type="hidden" name="id" id="id" value="<%= udata.id%>">
                    <label for=""><b>name:-</b></label>
                    <input type="text" name="name" value="<%= udata.name %>"><br><br>

                    <label for=""><b>Age:-</b></label>
                    <input type="number" name="age" value="<%= udata.age %>"><br><br>

                    <label for=""><b>email:-</b></label>
                    <input type="email" name="email" value="<%= udata.email %>"><br><br>

                    <label for=""><b>Address:-</b></label>
                    <input type="text" name="address" value="<%= udata.address %>"><br><br>

                    <label for=""><b>Image:-</b></label>
                    <input type="file" name="img"><br><br>

                    <input type="submit" class="btn btn-primary" value="Save" name="save">
                </form>
            </div>
            <div class="col-8 p-5 bg-warning  d-flex justify-content-center align-items-center">
                <br><br>

                <table border="2" class="table table-striped">
                    <tr align="center">
                        <td><b>id</b></td>
                        <td><b>Name</b></td>
                        <td><b>Age</b></td>
                        <td><b>Email</b></td>
                        <td><b>Address</b></td>
                        <td><b>Image</b></td>
                        <td><b>Action</b></td>
                    </tr>
                    <% data.forEach((i)=>{ %>
                        <tr align="center">
                            <td>
                                <%= i._id %>
                            </td>
                            <td>
                                <%= i.name %>
                            </td>
                            <td>
                                <%= i.age %>
                            </td>
                            <td>
                                <%= i.email %>
                            </td>
                            <td>
                                <%= i.address %>
                            </td>
                            <td>
                                <img src="../<%= i.img %>" alt="user image" height="100px" width="100px">
                            </td>
                            <td>
                                <a class="btn btn-success" href="/edit/<%= i.id %>">Edit</a> <a class="btn btn-danger"
                                    href="/del/<%= i.id%>">Delete</a>
                            </td>
                        </tr>
                        <%}) %>
                </table>
            </div>
        </div>
    </div>
</body>

</html>